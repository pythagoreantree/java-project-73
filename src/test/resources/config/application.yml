base-url: ""

spring:
  application:
    name: taskManager

  jpa:
    show-sql: true

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml

logging:
  level:
    root: INFO